<?xml version="1.0" ?>

<container xmlns="http://symfony.com/schema/dic/services"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://symfony.com/schema/dic/services http://symfony.com/schema/dic/services/services-1.0.xsd">

    <!--
    <parameters>
        <parameter key="{{ parameters.vendor.getValue|lower }}_{{ parameters.bundle.getValue|replace({'Bundle': ''})|lower }}.example.class">{{ parameters.vendor.getValue }}\{{ parameters.bundle.getValue }}\Example</parameter>
    </parameters>

    <services>
        <service id="{{ parameters.vendor.getValue|lower }}_{{ parameters.bundle.getValue|replace({'Bundle': ''})|lower }}.example" class="%{{ parameters.vendor.getValue|lower }}_{{ parameters.bundle.getValue|replace({'Bundle': ''})|lower }}.example.class%">
            <argument type="service" id="service_id" />
            <argument>plain_value</argument>
            <argument>%parameter_name%</argument>
        </service>
    </services>
    -->
</container>
